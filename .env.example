# Anthropic API key for menu recognition (upload/photo → dishes).
# Get one at https://console.anthropic.com → API Keys, then set this in
# Vercel: Project → Settings → Environment Variables → ANTHROPIC_API_KEY.
# For local dev, copy this file to .env.local and fill in the value.
ANTHROPIC_API_KEY=
